name: snackstack
description: Flutter package for pretty snackbars
version: 1.0.0
repository: https://github.com/justjew/snackstack

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
